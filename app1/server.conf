server {
    listen 80;
    server_name app1.EXAMPLE.NET;

    server_tokens off;

    index index.html;

    access_log /root/logs/access.log;
    error_log /root/logs/error.log;

    location / {
        root /root/public;
    }
}
